const lucide = require('lucide-react');
console.log(Object.keys(lucide).filter(k => k.toLowerCase().includes('help') || k.toLowerCase().includes('build') || k.toLowerCase().includes('scale') || k.toLowerCase().includes('ticket') || k.toLowerCase().includes('loader')));
