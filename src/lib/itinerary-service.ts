/**
 * @deprecated — Moved to @/services/itinerary
 * This shim exists to avoid breaking any imports not yet migrated.
 */
export { saveItinerary, type SaveItineraryPayload } from '@/services/itinerary';
